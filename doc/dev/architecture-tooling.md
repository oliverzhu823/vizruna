# Architecture & audit skills (pointer)

Full playbook (output paths, when to invoke FMSM / improve-codebase-architecture / architecture-copilot, constraints) lives in the **local** tree:

**`docs/dev/architecture-tooling.md`**

That file is gitignored with `docs/`. Clone for development only; copy the playbook to your machine or ask your agent to read it from disk.

**Public docs in this repo:** `doc/` (adapters, user guide) — do not put audit reports or Trellis spec here.

**Coding contracts (local):** `.trellis/spec/` — never force-add to GitHub.